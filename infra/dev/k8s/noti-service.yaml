apiVersion: apps/v1
kind: Deployment
metadata:
  name: noti-service
  namespace: default
  labels:
    app: noti-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: noti-service
  template:
    metadata:
      labels:
        app: noti-service
    spec:
      containers:
      - name: noti-service
        image: noti-service:latest
        imagePullPolicy: Never  # For local development with Tilt
        env:
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: RABBITMQ_URL
        - name: GMAIL_USER
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: GMAIL_USER
        - name: GMAIL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: GMAIL_PASSWORD
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
